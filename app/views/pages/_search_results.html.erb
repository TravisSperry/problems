<div style='margin-top:5%;'>
  <h2 style='display:inline;'>Results...</h2><%= link_to 'back', root_url, class: 'button button-primary u-pull-right' %>
  <hr>
  <div class='  row'>
    <% @projects.each do |project| %>
      <%= link_to project.name, project %>
      <hr>
    <% end %>
  </div>
</div>