<h1>Listing project_other_fields</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @project_other_fields.each do |project_other_field| %>
      <tr>
        <td><%= link_to 'Show', project_other_field %></td>
        <td><%= link_to 'Edit', edit_project_other_field_path(project_other_field) %></td>
        <td><%= link_to 'Destroy', project_other_field, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project other field', new_project_other_field_path %>
